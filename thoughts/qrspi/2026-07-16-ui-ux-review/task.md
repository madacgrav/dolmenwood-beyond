# Task

The app's UI is starting to feel clunky. Bring in a UI/UX expert perspective to
review the current front-end configuration — layout, navigation, styling system,
component consistency, and interaction patterns — and recommend concrete changes.

This is a review-and-recommend task, not a fixed feature build. The goal is a
prioritized set of UX improvements grounded in what the app actually does today.
